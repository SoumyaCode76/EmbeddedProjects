# STM32F429I Discovery Board Overlay

# Optional: Override LED configuration if needed
&led0 {
    /* Custom LED configuration if required */
};

# Optional: UART configuration for console
&usart1 {
    status = "okay";
    current-speed = <115200>;
};

# Optional: GPIO configuration
&gpiog {
    status = "okay";
};